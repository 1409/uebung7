
<%= link_to 'New Tak', new_tak_path, class: "btn btn-success btn-sm" %>

<h2>Todo(<%= pluralize(@todo.count, "Task") %>, <%= pluralize(@todo.sum("duration"), "Hour") %>)
</h2>
<%= render partial: "table", locals: {taks: @todo} %>
<h2>Done(<%= pluralize(@done.count, "Task") %>, <%= pluralize(@done.sum("duration"), "Hour") %>)</h2>
<%= render partial: "table", locals: {taks: @done} %>


<br>


